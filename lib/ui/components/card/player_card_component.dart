import 'package:flutter/material.dart';

class PlayerCardComponent extends StatelessWidget {
  const PlayerCardComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      width: 75,
      height: 150,
    );
  }
}
